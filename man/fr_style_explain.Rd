% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-style.R
\name{fr_style_explain}
\alias{fr_style_explain}
\title{Explain Style Resolution for a Cell}
\usage{
fr_style_explain(spec, row, col)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{row}{Integer scalar. Body row index (1-based).}

\item{col}{Character scalar or integer. Column name or column index.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \verb{$final} — named list of resolved properties (\code{bold}, \code{italic},
\code{fg}, \code{bg}, \code{align}, \code{valign}, \code{indent}, \code{font_size}).
\item \verb{$layers} — ordered list of matching styles with their index
in \code{spec$cell_styles}, type, and overridden properties.
}

Prints a human-readable summary to the console showing:
\enumerate{
\item The cell content value.
\item Each matching style layer with its index, type (col/row/cell),
and properties it sets.
\item The final resolved properties after all layers are applied.
}
}
\description{
Diagnostic tool that reports which cell styles affect a specific cell in the
table body, the order they are applied, and the final resolved properties.
Use this when a cell does not look as expected in the rendered output —
it shows exactly which style layers are active and how they combine.
}
\section{Interpreting the output}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{-- Style explain: row 1, col "total" --
Content: "135"
2 matching styles:
  [1] col: bg="#EBF5FB"
  [2] row: bold=TRUE

Final: bold=TRUE, italic=FALSE, fg=#000000, bg=#EBF5FB, ...
}\if{html}{\out{</div>}}

The \verb{[1]}, \verb{[2]} indices refer to the position in \code{spec$cell_styles}.
Properties from later layers override earlier ones. In this example,
the column style sets the background; the row style adds bold.
}

\examples{
spec <- tbl_demog |>
  fr_table() |>
  fr_hlines("header") |>
  fr_styles(
    fr_col_style(cols = "total", bg = "#EBF5FB"),
    fr_row_style(rows = 1L, bold = TRUE)
  )

# See which styles affect row 1, column "total"
fr_style_explain(spec, row = 1L, col = "total")

# Programmatic access to the resolved properties
result <- fr_style_explain(spec, row = 1L, col = "total")
result$final$bold   # TRUE (from row style)
result$final$bg     # "#EBF5FB" (from col style)

}
\seealso{
\code{\link[=fr_styles]{fr_styles()}} to apply styles, \code{\link[=fr_style]{fr_style()}} for cell-level
overrides, \code{\link[=fr_row_style]{fr_row_style()}} and \code{\link[=fr_col_style]{fr_col_style()}} for broader styles.
}
