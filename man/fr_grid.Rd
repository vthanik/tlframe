% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-rules.R
\name{fr_grid}
\alias{fr_grid}
\title{Apply Horizontal and Vertical Rules Together}
\usage{
fr_grid(
  spec,
  hpreset = "header",
  vpreset = "box",
  width = NULL,
  color = NULL,
  linestyle = NULL
)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{hpreset}{Horizontal rule preset passed to \code{\link[=fr_hlines]{fr_hlines()}}. Default
\code{"header"} (single rule below column header — standard TFL output).

Alternatively, pass a \strong{single slash-separated string} combining both
presets: \code{"header/box"}, \code{"booktabs/all"}, etc. When a \code{/} is detected,
the left side is used as \code{hpreset} and the right side as \code{vpreset}.}

\item{vpreset}{Vertical rule preset passed to \code{\link[=fr_vlines]{fr_vlines()}}. Default
\code{"box"} (outer border only). Ignored when \code{hpreset} contains a \code{/}.}

\item{width}{Rule width applied to \strong{both} horizontal and vertical rules.
Named shorthand (\code{"hairline"}, \code{"thin"}, \code{"medium"}, \code{"thick"}) or
numeric in points. \code{NULL} uses each preset's own default.}

\item{color}{Rule colour for both directions. \code{NULL} = black.}

\item{linestyle}{Line style for both directions. \code{NULL} = \code{"solid"}.}
}
\value{
A modified \code{fr_spec} with both horizontal and vertical rules set.
}
\description{
A convenience wrapper that calls \code{\link[=fr_hlines]{fr_hlines()}} and \code{\link[=fr_vlines]{fr_vlines()}} in one
step. Use \code{fr_grid()} when you want to set both rule directions at once
with consistent styling (same width, colour, and linestyle).

Prefer \code{fr_grid()} over chaining \code{fr_hlines() |> fr_vlines()} when:
\itemize{
\item You want uniform width/colour/linestyle for both directions.
\item You use the slash shorthand for concise one-liners: \code{fr_grid("header/box")}.
}

Prefer separate \code{fr_hlines()} + \code{fr_vlines()} calls when:
\itemize{
\item Horizontal and vertical rules need different widths, colours, or styles.
\item You need \code{fr_vlines(cols = ...)} for custom column positions.
}
}
\section{Common combinations}{
\tabular{llll}{
   Call \tab Horizontal \tab Vertical \tab Use case \cr
   \code{fr_grid()} \tab header \tab box \tab Pharma standard (most common) \cr
   \code{fr_grid("booktabs/box")} \tab booktabs \tab box \tab Publication style \cr
   \code{fr_grid("hsides/all")} \tab top+bottom \tab all columns \tab Full grid \cr
   \code{fr_grid("open/box")} \tab above+below header \tab box \tab Header-framed \cr
   \code{fr_grid("void/void")} \tab none \tab none \tab Clear all rules \cr
}
}

\examples{
## ── Standard pharma output (header + box) ─────────────────────────────

tbl_demog |> fr_table() |> fr_grid()

## ── Slash shorthand ───────────────────────────────────────────────────

# Equivalent to fr_grid("booktabs", "box")
tbl_demog |> fr_table() |> fr_grid("booktabs/box")

## ── Full grid (top/bottom + all column separators) ────────────────────

tbl_demog |> fr_table() |> fr_grid("hsides/all")

## ── Custom width applied uniformly ────────────────────────────────────

tbl_demog |> fr_table() |> fr_grid("header/box", width = "medium")

## ── Full pipeline ─────────────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_titles("Table 14.1.1 Demographics") |>
  fr_grid("header/box") |>
  fr_page(orientation = "landscape")

}
\seealso{
\code{\link[=fr_hlines]{fr_hlines()}} for horizontal rules only, \code{\link[=fr_vlines]{fr_vlines()}} for
vertical rules only.
}
