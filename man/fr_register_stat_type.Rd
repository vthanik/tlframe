% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants.R
\name{fr_register_stat_type}
\alias{fr_register_stat_type}
\title{Register a Custom Decimal Alignment Stat Type}
\usage{
fr_register_stat_type(name, pattern, family = "custom", richness = 3L)
}
\arguments{
\item{name}{Character scalar. Unique name for the type (e.g.,
\code{"ratio_ci"}). Must not conflict with built-in type names.}

\item{pattern}{Character scalar. Perl-compatible regex that matches
the formatted string. Must match the \strong{entire} string
(anchored with \verb{^...$}).}

\item{family}{Character scalar. Alignment family: \code{"compound"},
\code{"estimate"}, \code{"range"}, \code{"count"}, \code{"float"}, or \code{"custom"}.
Determines how the type aligns relative to other types in the same
column. Default \code{"custom"}.}

\item{richness}{Integer scalar. Component count (higher = more complex).
Used for tie-breaking when multiple types match. Default \code{3L}.}
}
\value{
Invisible \code{NULL}. Called for its side effect of registering the type.
}
\description{
Adds a custom statistical format type to the decimal alignment engine.
Use this when your data contains formatted statistics that don't match
any of the 18 built-in types (see \code{.arframe_registry$stat_type_registry} for the
full list).
}
\examples{
# Register a custom "ratio (lower, upper)" format
fr_register_stat_type(
  name = "ratio_ci",
  pattern = "^-?\\\\d+\\\\.?\\\\d*\\\\s*\\\\(-?\\\\d+\\\\.?\\\\d*,\\\\s*-?\\\\d+\\\\.?\\\\d*\\\\)$",
  family = "compound",
  richness = 4L
)

}
\seealso{
\code{\link[=fr_cols]{fr_cols()}} with \code{align = "decimal"} for decimal alignment.
}
