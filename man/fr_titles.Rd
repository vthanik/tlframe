% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-content.R
\name{fr_titles}
\alias{fr_titles}
\title{Set Table Titles}
\usage{
fr_titles(spec, ..., .align = "center", .bold = FALSE, .font_size = NULL)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{...}{One or more title lines. Each may be:
\itemize{
\item A \strong{character scalar} — plain text, optionally with \verb{\{fr_*()\}} markup.
\item A \strong{list}. The first element is the text content (can be unnamed).
Optionally include \code{align}, \code{bold}, \code{font_size} to override per-line styling.
}}

\item{.align}{Default alignment for all title lines. One of \code{"left"},
\code{"center"} (default), or \code{"right"}. Can be overridden per line via the
list form.}

\item{.bold}{Default bold for all title lines. \code{FALSE} by default.}

\item{.font_size}{Default font size in points. \code{NULL} inherits the page
font size set by \code{\link[=fr_page]{fr_page()}}.}
}
\value{
A modified \code{fr_spec}. Titles are stored in \code{spec$meta$titles}.
}
\description{
Sets one or more title lines displayed above the table body. Each argument
in \code{...} produces one title line in the rendered output. Calling
\code{fr_titles()} again \strong{replaces} all previously set titles.

Titles support inline \verb{\{fr_*()\}} markup — see \code{\link[=fr_bold]{fr_bold()}}, \code{\link[=fr_italic]{fr_italic()}},
\code{\link[=fr_super]{fr_super()}}, \code{\link[=fr_unicode]{fr_unicode()}}.
}
\section{Regulatory conventions}{

ICH E3 §10 specifies that each table should carry:
\itemize{
\item \strong{Line 1} — Table number and title (e.g. \code{"Table 14.1.1 Summary of Demographics and Baseline Characteristics"}).
\item \strong{Line 2} — Population qualifier (e.g. \code{"Full Analysis Set"} or
\code{"Safety Population"}).
\item \strong{Line 3} — Study identifier or further qualifier (optional, e.g.
\code{"Study TFRM-2024-001"} or \code{"Database Cutoff: 31DEC2024"}).
}

\strong{Standard table numbering scheme (ICH E3 / FDA/EMA submissions):}\tabular{ll}{
   Section \tab Content \cr
   14.1.x \tab Demographics, disposition, baseline characteristics \cr
   14.2.x \tab Efficacy results (including PK/PD) \cr
   14.3.1.x \tab Adverse events & dosing/exposure \cr
   14.3.2.x \tab Deaths, serious & significant adverse events \cr
   14.3.4.x \tab Abnormal laboratory values \cr
   14.3.5.x \tab Vital signs, ECG, physical findings \cr
   16.2.x \tab Subject data listings \cr
}


In TFL outputs, titles are typically \strong{left-aligned}, 9 pt, and
not bold. Use \code{"center"} alignment for PDF or journal output.
}

\section{Spacing}{

By default, \strong{one blank line} is inserted after the last title and before
the column header. Control this with \code{\link[=fr_spacing]{fr_spacing()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{spec |> fr_spacing(titles_after = 0L)   # no gap
spec |> fr_spacing(titles_after = 2L)   # two blank lines
}\if{html}{\out{</div>}}

This can also be set in \verb{_tlframe.yml}:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{spacing:
  titles_after: 1
}\if{html}{\out{</div>}}
}

\section{Tips}{

\itemize{
\item Pass 0 arguments (\code{fr_titles(spec)}) to clear all titles.
\item Markup in titles: use \code{"{fr_bold('Table 14.1.1')}"} to bold just the
table number. The glue \code{{...}} syntax is evaluated at spec-build time.
\item \code{fr_titles()} always \strong{replaces} — chain multiple calls if you want to
progressively build a pipeline, but only the last call takes effect.
}
}

\examples{
## ── Standard two-line ICH E3 title ──────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_titles(
    "Table 14.1.1 Summary of Demographics and Baseline Characteristics",
    "Full Analysis Set"
  )

## ── Three-line title with study identifier ───────────────────────────────

tbl_ae_soc |>
  fr_table() |>
  fr_titles(
    "Table 14.3.2 Adverse Events by System Organ Class and Preferred Term",
    "Safety Analysis Set",
    "Study TFRM-2024-001"
  )

## ── Inline markup in a title ─────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_titles(
    "{fr_bold('Table 14.1.1')} Summary of Demographics",
    "Full Analysis Set"
  )

## ── Per-line styling with list form ──────────────────────────────────────

# List form for per-line styling overrides
tbl_demog |>
  fr_table() |>
  fr_titles(
    list("Table 14.1.1 Summary of Demographics", bold = TRUE, font_size = 10),
    list("Full Analysis Set", align = "left", font_size = 9)
  )

## ── Left-aligned titles (common in pharma RTF) ───────────────────────────

tbl_demog |>
  fr_table() |>
  fr_titles(
    "Table 14.1.1 Summary of Demographics",
    "Full Analysis Set",
    .align = "left"
  )

## ── Clear all titles ─────────────────────────────────────────────────────

spec <- tbl_demog |> fr_table() |> fr_titles("Old Title")
spec <- spec |> fr_titles()  # removes all titles

}
\seealso{
\code{\link[=fr_footnotes]{fr_footnotes()}} for footnotes below the table,
\code{\link[=fr_pagehead]{fr_pagehead()}} for running page headers, \code{\link[=fr_spacing]{fr_spacing()}} for gap
control, \code{\link[=fr_bold]{fr_bold()}} and \code{\link[=fr_italic]{fr_italic()}} for inline markup.
}
