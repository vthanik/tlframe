% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-rules.R
\name{fr_hlines}
\alias{fr_hlines}
\title{Apply Horizontal Rules}
\usage{
fr_hlines(
  spec,
  preset = "header",
  width = NULL,
  color = NULL,
  linestyle = NULL
)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{preset}{Named rule preset. One of:
\itemize{
\item \code{"header"} — single rule below the column header
only. \strong{Default and most common in TFL outputs} (ICH E3).
No top or bottom border; footnotes follow the last body row directly.
\item \code{"open"} — rule above header + rule below header; no bottom border.
Use when the table bottom is immediately followed by footnotes.
\item \code{"hsides"} — rule at the top (above header) and bottom (below body)
only.
\item \code{"above"} — single rule above the column header only.
\item \code{"below"} — single rule below the last body row only.
\item \code{"box"} — full outer border on all four sides.
\item \code{"booktabs"} — thick top (1 pt) + thin mid (0.5 pt) + thick bottom
(1 pt). The standard for PDF and publication output.
\item \code{"void"} — no horizontal rules. Clears all previously set rules.
}}

\item{width}{Rule width. Named shorthand or numeric in points:
\itemize{
\item \code{"hairline"} = 0.25 pt
\item \code{"thin"} = 0.5 pt (default preset width)
\item \code{"medium"} = 1.0 pt
\item \code{"thick"} = 1.5 pt
\item Numeric, e.g. \code{width = 0.75} (positive, in pt)
}

When supplied, overrides the preset's default widths for \strong{all} rules
created by this call. \code{NULL} uses each preset's own default widths.}

\item{color}{Rule colour: hex string (\code{"#003366"}) or CSS named colour
(\code{"navy"}). \code{NULL} = black (\code{"#000000"}).}

\item{linestyle}{Rule line style. One of \code{"solid"} (default), \code{"dashed"},
\code{"dotted"}, \code{"dashdot"}, or \code{"double"}.}
}
\value{
A modified \code{fr_spec}. Rules stored in \code{spec$rules}.
}
\description{
Sets horizontal rules (lines) for the table using a named preset or
custom rule properties. Calling \code{fr_hlines()} again \strong{replaces} all
previously set horizontal rules.
}
\section{Regulatory conventions}{

Most pharma TFL outputs use \code{"header"}: a single thin
solid rule separating column headers from body. No top or bottom border.
Footnotes are placed directly below the last body row with a short
separator line drawn by the footnote block itself (controlled by the
\code{.separator} argument of \code{\link[=fr_footnotes]{fr_footnotes()}}).

\code{"booktabs"} is the standard for PDF/publication output and matches the
LaTeX \verb{\\toprule} / \verb{\\midrule} / \verb{\\bottomrule} pattern.
}

\section{Tips}{

\itemize{
\item Combine \code{fr_hlines()} and \code{fr_vlines()} independently — each manages its
own set of rules.
\item \code{fr_hlines("void")} clears \strong{only} horizontal rules; vertical rules from
\code{fr_vlines()} are not affected.
\item \code{width}, \code{color}, and \code{linestyle} apply uniformly to \strong{all} rules created
by this preset call. For mixed widths (e.g. booktabs thick/thin), omit
\code{width} and let the preset define its own widths.
}
}

\examples{
## ── Common presets ────────────────────────────────────────────────────────

# Standard TFL output: single rule under column header
tbl_demog |> fr_table() |> fr_hlines("header")

# Open: header rules, no bottom border
tbl_demog |> fr_table() |> fr_hlines("open")

# Booktabs: publication-quality thick/thin/thick
tbl_demog |> fr_table() |> fr_hlines("booktabs")

# Top and bottom rules only
tbl_demog |> fr_table() |> fr_hlines("hsides")

# Box: full outer border
tbl_demog |> fr_table() |> fr_hlines("box")

# Remove all horizontal rules
tbl_demog |> fr_table() |> fr_hlines("void")

## ── Custom width ─────────────────────────────────────────────────────────

# Hairline rules (0.25 pt)
tbl_demog |> fr_table() |> fr_hlines("header", width = "hairline")

# Thick rules (1.5 pt)
tbl_demog |> fr_table() |> fr_hlines("hsides", width = "thick")

# Exact width in points
tbl_demog |> fr_table() |> fr_hlines("header", width = 0.75)

## ── Custom colour ─────────────────────────────────────────────────────────

# Navy blue rules
tbl_demog |> fr_table() |> fr_hlines("booktabs", color = "#003366")

## ── Custom linestyle ──────────────────────────────────────────────────────

# Dashed rule below header
tbl_demog |> fr_table() |> fr_hlines("header", linestyle = "dashed")

# Double rule below header
tbl_demog |> fr_table() |> fr_hlines("header", linestyle = "double")

# Dash-dot (SAS ODS equivalent)
tbl_demog |> fr_table() |> fr_hlines("header", linestyle = "dashdot")

## ── Full pipeline ─────────────────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_titles("Table 14.1.1 Demographics", "Full Analysis Set") |>
  fr_footnotes("[a] Percentages based on N in column header.") |>
  fr_hlines("header") |>
  fr_vlines("box")

}
\seealso{
\code{\link[=fr_vlines]{fr_vlines()}} for vertical rules, \code{\link[=fr_styles]{fr_styles()}} for cell shading
and font overrides, \code{\link[=fr_footnotes]{fr_footnotes()}} for the \code{.separator} control.
}
