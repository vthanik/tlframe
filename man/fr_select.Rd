% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-cols.R
\name{fr_select}
\alias{fr_select}
\title{Select Columns by Name Pattern}
\usage{
fr_select(spec, pattern = NULL, cols = NULL, exclude = NULL)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{pattern}{A regular expression (passed to \code{\link[=grep]{grep()}}). Selects all
column names in \code{spec$data} that match. \code{NULL} returns all columns
(unless \code{cols} is also supplied).}

\item{cols}{A character vector of exact column names to include.
When both \code{cols} and \code{pattern} are supplied, their results are
\strong{unioned} in data-frame column order.}

\item{exclude}{Character vector of column names to \strong{remove} from the
result. Applied after pattern/cols matching.}
}
\value{
A character vector of column names in data-frame column order.
}
\description{
A lightweight column selection helper that returns a character vector of
column names from an \code{fr_spec}, suitable for passing to the \code{cols}
argument of \code{\link[=fr_col_style]{fr_col_style()}}, \code{\link[=fr_style]{fr_style()}}, \code{\link[=fr_spans]{fr_spans()}}, or any verb
that accepts column names.

\code{fr_select()} uses base R \code{\link[=grep]{grep()}} — no tidyselect dependency. For
\code{tidyselect}-style helpers (\code{starts_with()}, \code{ends_with()}, etc.),
use the formula interface in \code{\link[=fr_cols]{fr_cols()}} instead.

Use \code{fr_select()} when you need a \strong{reusable} column vector — for
example, to pass the same set of columns to both \code{fr_spans()} and
\code{fr_col_style()}. For one-off column targeting, inline the names
directly in \code{cols = c("a", "b")}.
}
\section{Tips}{

\itemize{
\item \code{fr_select(spec)} with no arguments returns \strong{all} column names —
useful as a starting point for exclusion-based selection.
\item Combine \code{pattern} and \code{exclude} for expressive selections:
\code{fr_select(spec, pattern = "^zom_", exclude = "zom_total")}.
\item The result is always in data-frame column order, regardless of the
order you list names in \code{cols}.
}
}

\examples{
spec <- tbl_demog |> fr_table()

## ── All columns ──────────────────────────────────────────────────────────

fr_select(spec)

## ── Regex: columns starting with "zom_" ──────────────────────────────────

fr_select(spec, pattern = "^zom_")

## ── Exact names ──────────────────────────────────────────────────────────

fr_select(spec, cols = c("zom_50mg", "zom_100mg"))

## ── Exclude a column ─────────────────────────────────────────────────────

fr_select(spec, exclude = "characteristic")

## ── Use result in fr_col_style ────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_styles(
    fr_col_style(cols = fr_select(spec, pattern = "^zom_"), align = "right")
  )

}
\seealso{
\code{\link[=fr_cols]{fr_cols()}} for column configuration, \code{\link[=fr_col_style]{fr_col_style()}} for
column styling.
}
