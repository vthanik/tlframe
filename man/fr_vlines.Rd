% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-rules.R
\name{fr_vlines}
\alias{fr_vlines}
\title{Apply Vertical Rules}
\usage{
fr_vlines(
  spec,
  preset = "box",
  cols = NULL,
  width = NULL,
  color = NULL,
  linestyle = NULL,
  abovepos = NULL,
  belowpos = NULL
)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{preset}{Named rule preset or \code{"void"} (no vertical rules):
\itemize{
\item \code{"box"} — rules on the leftmost and rightmost table edges only (outer
vertical borders). Default.
\item \code{"all"} — rules between every column and on outer edges.
\item \code{"inner"} — rules between columns only; no outer edges.
\item \code{"void"} — no vertical rules. Clears all previously set vlines.
}}

\item{cols}{Integer vector of column \strong{gap} positions to rule.
Position \code{j} draws a rule to the right of column \code{j}. Use alongside
\code{preset = "void"} to add individual column separators. \code{NULL} uses the
preset.}

\item{width}{Rule width. Named shorthand (\code{"hairline"}, \code{"thin"}, \code{"medium"},
\code{"thick"}) or numeric in points. Default \code{NULL} (thin, 0.5 pt).}

\item{color}{Rule colour: hex string or CSS named colour. \code{NULL} = black.}

\item{linestyle}{One of \code{"solid"} (default), \code{"dashed"}, \code{"dotted"},
\code{"dashdot"}, \code{"double"}.}

\item{abovepos, belowpos}{Fractions (0–1) controlling partial vertical extent
of the rule within a row. \code{NULL} = full height (default).}
}
\value{
A modified \code{fr_spec}. Vertical rules stored in \code{spec$rules}.
}
\description{
Sets vertical rules (column separators) for the table. Calling
\code{fr_vlines()} again \strong{replaces} all previously set vertical rules.
Horizontal rules set by \code{\link[=fr_hlines]{fr_hlines()}} are not affected.
}
\section{Preset comparison}{
\tabular{lllll}{
   Preset \tab Left edge \tab Between cols \tab Right edge \tab Use case \cr
   \code{"box"} \tab Yes \tab No \tab Yes \tab Clean outer border (most common) \cr
   \code{"all"} \tab Yes \tab Yes \tab Yes \tab Full grid with column separators \cr
   \code{"inner"} \tab No \tab Yes \tab No \tab Column separators, open sides \cr
   \code{"void"} \tab No \tab No \tab No \tab No vertical rules \cr
}
}

\section{Regulatory conventions}{

Most pharma TFL outputs use \strong{no vertical rules} (\code{"void"}) or just an
outer box border (\code{"box"}). Full grids (\code{"all"}) are uncommon in
regulatory submissions but may be used for dense listing tables or
shift tables where column boundaries aid readability.
}

\section{Tips}{

\itemize{
\item \code{fr_vlines("box")} is the most common use — a clean outer border.
\item Combine with \code{fr_hlines("header")} for the typical pharma style:
box outer border + single rule under the column header.
\item \code{cols = c(1L, 3L)} draws a rule to the right of columns 1 and 3 —
useful for separating a stub column from data columns.
\item \code{fr_vlines()} and \code{fr_hlines()} are independent — each manages its
own rules. To set both at once, use \code{\link[=fr_grid]{fr_grid()}}.
}
}

\examples{
## ── Common presets ────────────────────────────────────────────────────────

# Outer box border only (most common)
tbl_demog |> fr_table() |> fr_vlines("box")

# Rules between every column
tbl_demog |> fr_table() |> fr_vlines("all")

# Inner column separators only (no outer edges)
tbl_demog |> fr_table() |> fr_vlines("inner")

# Remove all vertical rules
tbl_demog |> fr_table() |> fr_vlines("void")

## ── Custom column positions ───────────────────────────────────────────────

# Rule after the stub column (column 1) only
tbl_demog |>
  fr_table() |>
  fr_vlines("void") |>
  fr_vlines(cols = 1L)

## ── Custom width, style, and colour ────────────────────────────────────────

# Thick outer box
tbl_demog |> fr_table() |> fr_vlines("box", width = "thick")

# Dashed inner separators
tbl_demog |> fr_table() |> fr_vlines("all", linestyle = "dashed")

# CSS named colour
tbl_demog |> fr_table() |> fr_vlines("box", color = "slategray")

## ── Combined with fr_hlines ───────────────────────────────────────────────

# Typical regulatory pharma style
tbl_demog |>
  fr_table() |>
  fr_hlines("header") |>
  fr_vlines("box")

# Full grid: all borders
tbl_demog |>
  fr_table() |>
  fr_hlines("hsides") |>
  fr_vlines("all")

}
\seealso{
\code{\link[=fr_hlines]{fr_hlines()}} for horizontal rules, \code{\link[=fr_grid]{fr_grid()}} to set both in
one call, \code{\link[=fr_styles]{fr_styles()}} for cell shading and font styling.
}
