% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markup.R
\name{fr_sub}
\alias{fr_sub}
\title{Subscript Markup}
\usage{
fr_sub(x)
}
\arguments{
\item{x}{Character or numeric. Content to render as subscript.
Coerced to character internally.}
}
\value{
An \code{fr_markup} object. When interpolated inside a glue string,
produces a sentinel token resolved at render time.
}
\description{
Renders content as subscript in the final output. Use inside
glue-string expressions in titles, footnotes, column labels, and cell
values. Common in clinical statistics for chemical formulas (H₂O),
statistical notation (beta-1), and indexed variables (x_i).
}
\section{Rendering}{

In RTF output, subscript uses \verb{\\sub} (half the font size, lowered).
In future LaTeX output, it maps to \verb{\\textsubscript\{\}}. The subscript
text renders at roughly half the surrounding font size.
}

\examples{
# Chemical formula: H₂O
fr_col("H{fr_sub(2)}O", width = 1.0)

# Statistical subscript: x_i
fr_col("x{fr_sub('i')}", width = 1.0)

# In a footnote (requires a spec):
\dontrun{
spec |> fr_footnotes("{fr_sub('n')} = number of subjects with data")
}

}
\seealso{
\code{\link[=fr_super]{fr_super()}} for superscript, \code{\link[=fr_unicode]{fr_unicode()}} for arbitrary
Unicode characters.
}
