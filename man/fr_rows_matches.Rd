% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-style.R
\name{fr_rows_matches}
\alias{fr_rows_matches}
\title{Select Rows by Column Value or Pattern}
\usage{
fr_rows_matches(col, value = NULL, pattern = NULL, ignore.case = FALSE)
}
\arguments{
\item{col}{Character scalar. Name of the data column to match against.
Must exist in the data frame passed to \code{\link[=fr_table]{fr_table()}}.}

\item{value}{Scalar. Exact value to match (using \code{==}). Mutually exclusive
with \code{pattern}. Supports any atomic type (character, numeric, logical).}

\item{pattern}{Character scalar. A regular expression passed to \code{\link[=grep]{grep()}}
(Perl-compatible). Mutually exclusive with \code{value}. The regex is
matched against the character representation of the column values.}

\item{ignore.case}{Logical. Whether \code{pattern} matching is case-insensitive.
Default \code{FALSE}. Ignored when \code{value} is used.}
}
\value{
An \code{fr_rows_selector} object for use in \code{\link[=fr_row_style]{fr_row_style()}} or
\code{\link[=fr_style]{fr_style()}}.
}
\description{
Creates a row selector object for use in the \code{rows} argument of
\code{\link[=fr_row_style]{fr_row_style()}} or \code{\link[=fr_style]{fr_style()}}. At style-application time (inside
\code{\link[=fr_styles]{fr_styles()}}), the selector is evaluated against the table data frame to
produce the matching row positions.

This avoids hard-coding integer row numbers when styling rows by their
content — for example, bolding every "Total" row or colouring every
p-value row red. The selector is data-driven: if rows are reordered or
filtered, the styles automatically track the correct rows.
}
\section{Common patterns}{
\tabular{ll}{
   Pattern \tab Matches \cr
   \code{value = "Total"} \tab Exact string "Total" \cr
   \code{pattern = "^Total"} \tab Starts with "Total" \cr
   \code{pattern = "p[- .]?value"} \tab "p-value", "p value", "p.value" \cr
   \code{pattern = "^\\\\\\\\s"} \tab Rows starting with whitespace (indented) \cr
   \code{pattern = "^[A-Z]"} \tab Rows starting with uppercase (group headers) \cr
}
}

\examples{
## ── Bold every "Total" row ────────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_styles(
    fr_row_style(rows = fr_rows_matches("characteristic", "Total"), bold = TRUE)
  )

## ── Red text for p-value rows (regex) ────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_styles(
    fr_row_style(
      rows = fr_rows_matches("characteristic", pattern = "^p[- .]?value",
                              ignore.case = TRUE),
      fg = "#CC0000", italic = TRUE
    )
  )

## ── Combine with cell styles ──────────────────────────────────────────────

tbl_ae_soc |>
  fr_table() |>
  fr_styles(
    # Shade every SOC header row
    fr_row_style(rows = fr_rows_matches("row_type", "soc"), bg = "#F0F0F0", bold = TRUE),
    # Red text for all PT rows with a p-value pattern
    fr_row_style(rows = fr_rows_matches("row_type", "pt"), fg = "#333333")
  )

}
\seealso{
\code{\link[=fr_row_style]{fr_row_style()}} for row styling, \code{\link[=fr_style]{fr_style()}} for cell styling,
\code{\link[=fr_styles]{fr_styles()}} to apply styles to a spec.
}
