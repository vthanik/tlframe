% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-theme.R
\name{fr_theme_get}
\alias{fr_theme_get}
\title{Get the Current Study-Level Table Theme}
\usage{
fr_theme_get()
}
\value{
A named list of current theme settings, or an empty list if no
theme has been set. Possible keys (all optional):
\itemize{
\item \code{orientation} — \code{"landscape"} or \code{"portrait"}
\item \code{paper} — \code{"letter"}, \code{"a4"}, \code{"legal"}
\item \code{font_family} — e.g. \code{"Courier New"}
\item \code{font_size} — numeric (points)
\item \code{margins} — numeric vector (inches)
\item \code{tokens} — named list of token values
\item \code{pagehead} — list with \code{left}, \code{center}, \code{right}, \code{font_size}, \code{bold}
\item \code{pagefoot} — list with \code{left}, \code{center}, \code{right}, \code{font_size}, \code{bold}
\item \code{hlines} — preset name (e.g. \code{"header"})
\item \code{vlines} — preset name (e.g. \code{"box"})
\item \code{spacing} — list with \code{titles_after}, \code{footnotes_before},
\code{pagehead_after}, \code{pagefoot_before}, \code{page_by_after} (integer
blank lines)
\item \code{footnote_separator} — logical
}
}
\description{
Returns the current study-level theme as a named list. Use this to inspect
what defaults have been set via \code{\link[=fr_theme]{fr_theme()}}.
}
\examples{
fr_theme(font_size = 9, hlines = "header", orientation = "landscape")
fr_theme_get()
# $font_size
# [1] 9
# $hlines
# [1] "header"
# $orientation
# [1] "landscape"

# Programmatic access
theme <- fr_theme_get()
theme$font_size   # 9

## ── Empty state after reset ───────────────────────────────────────────────

fr_theme_reset()
fr_theme_get()     # list() — no theme settings active
length(fr_theme_get()) == 0L   # TRUE

}
\seealso{
\code{\link[=fr_theme]{fr_theme()}} to set, \code{\link[=fr_theme_reset]{fr_theme_reset()}} to clear,
\code{\link[=fr_config_get]{fr_config_get()}} for YAML config inspection.
}
