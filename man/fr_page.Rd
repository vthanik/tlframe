% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-page.R
\name{fr_page}
\alias{fr_page}
\title{Set Page Layout Options}
\usage{
fr_page(
  spec,
  orientation = NULL,
  paper = NULL,
  margins = NULL,
  font_family = NULL,
  font_size = NULL,
  orphan_min = NULL,
  widow_min = NULL,
  continuation = NULL,
  col_split = NULL,
  stub_cols = NULL,
  tokens = NULL
)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{orientation}{\code{"landscape"} (default, 11 × 8.5 in for Letter) or
\code{"portrait"} (8.5 × 11 in).}

\item{paper}{\code{"letter"} (default, 8.5 × 11 in), \code{"a4"} (210 × 297 mm),
or \code{"legal"} (8.5 × 14 in).}

\item{margins}{Margin(s) in inches. Accepts:
\itemize{
\item Scalar: \code{1} — all four sides equal.
\item Length 2: \code{c(vertical, horizontal)} — top/bottom vs left/right.
\item Length 4: \code{c(top, right, bottom, left)} — CSS order.
\item Named list: \code{list(top=1, bottom=1, left=0.75, right=0.75)}.
}}

\item{font_family}{Font family name. Defaults to the OS monospace font
(\code{"Courier New"} on Windows/macOS, \code{"Liberation Mono"} on Linux).
Regulatory submissions typically use Courier New 9pt.}

\item{font_size}{Font size in points. Default \code{9}. Typical pharma range:
7–10 pt.}

\item{orphan_min}{Minimum body rows to keep at the \strong{bottom} of a page
before forcing a page break. Default \code{3L}. Set to \code{1L} to disable.}

\item{widow_min}{Minimum body rows to carry to the \strong{top} of the next page.
Default \code{3L}. Set to \code{1L} to disable.}

\item{continuation}{Character scalar appended to the column header block on
continuation pages, e.g. \code{"(continued)"}. \code{NULL} (default) disables.}

\item{col_split}{Logical. Whether to split wide tables across multiple
column pages. Default \code{FALSE}.}

\item{stub_cols}{Character vector of column names to repeat on each
column-split page (e.g. the row-label column).}

\item{tokens}{Named list of custom \code{{token}} values for use in page
headers/footers. E.g. \code{list(study = "TFRM-2024-001", pop = "FAS")}.
Use these tokens in \code{\link[=fr_pagehead]{fr_pagehead()}} and \code{\link[=fr_pagefoot]{fr_pagefoot()}} strings.}
}
\value{
A modified \code{fr_spec}. Page settings stored in \code{spec$page}.
}
\description{
Sets page-level properties: paper size, orientation, margins, font, and
pagination controls. Only the arguments you \strong{explicitly supply} are
changed — all others retain their current values. This allows incremental
updates anywhere in a pipeline.
}
\section{Regulatory conventions}{

\strong{FDA/US submissions (eCTD):} landscape Letter (8.5 × 11 in),
Courier New 9 pt, 1 in margins on all sides. This is the most common
regulatory RTF standard and the package default.

\strong{EMA/European submissions:} A4 paper (210 × 297 mm) is acceptable;
landscape A4 is common for wide safety tables.

\strong{Database cutoff date} is a mandatory element in every CSR table.
The standard practice is to define it as a custom token:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fr_page(tokens = list(cutoff = "31DEC2024")) |>
fr_pagehead(left  = "Study: \{study\}",
            right = "Database Cutoff: \{cutoff\}")
}\if{html}{\out{</div>}}

\strong{Continuation label:} multi-page tables must indicate continuation.
Regulatory convention is to append \code{"(continued)"} to the column header
block on all pages after the first:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{fr_page(continuation = "(continued)")
}\if{html}{\out{</div>}}
}

\section{Tips}{

\itemize{
\item \code{tokens} are the clean way to inject study-level metadata (study number,
cutoff date, population) into running headers without repeating the
value in every \code{fr_pagehead()} call.
\item \code{orphan_min = 3} means: if fewer than 3 rows would remain at the bottom
of a page before a \code{group_by} group, move the group to the next page.
}
}

\examples{
## ── Standard pharma RTF setup ────────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_page(
    orientation = "landscape",
    paper       = "letter",
    font_family = "Courier New",
    font_size   = 9,
    margins     = 1
  )

## ── A4 portrait for European submissions ─────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_page(orientation = "portrait", paper = "a4", font_size = 9)

## ── Narrow margins to fit wide table ─────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_page(margins = c(1, 0.5))   # top/bottom = 1in, left/right = 0.5in

## ── Custom tokens for running header ─────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_page(tokens = list(study = "TFRM-2024-001", pop = "FAS")) |>
  fr_pagehead(left  = "Study: {study}  Population: {pop}",
              right = "Page {thepage} of {total_pages}")

## ── Continuation label on multi-page tables ──────────────────────────────

tbl_ae_soc |>
  fr_table() |>
  fr_page(continuation = "(continued)")

## ── Tight orphan/widow control ────────────────────────────────────────────

tbl_ae_soc |>
  fr_table() |>
  fr_page(orphan_min = 2L, widow_min = 2L)

}
\seealso{
\code{\link[=fr_pagehead]{fr_pagehead()}}, \code{\link[=fr_pagefoot]{fr_pagefoot()}} for running headers/footers,
\code{\link[=fr_rows]{fr_rows()}} for \code{page_by} / \code{group_by} row pagination.
}
