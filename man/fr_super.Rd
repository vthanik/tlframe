% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markup.R
\name{fr_super}
\alias{fr_super}
\title{Superscript Markup}
\usage{
fr_super(x)
}
\arguments{
\item{x}{Character or numeric. Content to render as superscript.
Coerced to character internally.}
}
\value{
An \code{fr_markup} object. When interpolated inside a glue string
(e.g. \code{"{fr_super(1)}"}), produces a sentinel token
(\verb{\\x01SUPER:1\\x02}) that is resolved at render time.
}
\description{
Renders content as superscript in the final output. Use inside
glue-string expressions in titles, footnotes, column labels, and cell
values. At render time the backend converts the markup to the
appropriate format (RTF control words or LaTeX \verb{\\textsuperscript\{\}}).

The most common use in regulatory tables is for footnote markers:
\code{"[a]{fr_super('a')}"} in a cell value, paired with
\code{"[a] Percentages based on N."} in \code{\link[=fr_footnotes]{fr_footnotes()}}.
}
\examples{
# Footnote marker
fr_super("a")

# Unit with exponent: kg/m²
fr_col("BMI kg/m{fr_super(2)}", width = 1.5)

# Standalone usage (inspect the sentinel)
format(fr_super(1))

# In a pipeline:
spec <- tbl_demog |> fr_table()
n <- 1
spec |> fr_footnotes("{fr_super(n)} Pearson chi-square test")

}
\seealso{
\code{\link[=fr_sub]{fr_sub()}} for subscript, \code{\link[=fr_bold]{fr_bold()}} and \code{\link[=fr_italic]{fr_italic()}} for
font style, \code{\link[=fr_dagger]{fr_dagger()}} for the † symbol.
}
