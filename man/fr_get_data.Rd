% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-accessors.R
\name{fr_get_data}
\alias{fr_get_data}
\title{Get the Data Frame from a Spec}
\usage{
fr_get_data(spec)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}} or \code{\link[=fr_listing]{fr_listing()}}.}
}
\value{
A data frame. For figure specs created by \code{\link[=fr_figure]{fr_figure()}}, returns
an empty data frame with zero rows and zero columns.
}
\description{
Extracts the data frame stored in the \code{fr_spec} object. This is the same
data frame passed to \code{\link[=fr_table]{fr_table()}} or \code{\link[=fr_listing]{fr_listing()}}, before any rendering
transformations (sorting, repeat suppression, blank-after rows).

Modifying the returned data frame does not affect the spec
(R copy-on-modify semantics protect the original).
}
\examples{
spec <- tbl_demog |> fr_table()
d <- fr_get_data(spec)
nrow(d)
names(d)

}
\seealso{
\code{\link[=fr_table]{fr_table()}}, \code{\link[=fr_listing]{fr_listing()}}, \code{\link[=fr_get_columns]{fr_get_columns()}} for column
configuration.
}
