% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markup.R
\name{fr_bold}
\alias{fr_bold}
\title{Bold Markup}
\usage{
fr_bold(x)
}
\arguments{
\item{x}{Character. Text to render in bold.}
}
\value{
An \code{fr_markup} object. When interpolated inside a glue string,
produces a sentinel token resolved at render time.
}
\description{
Renders a text span as bold in the final output. Use inside
glue-string expressions to apply bold formatting to a \strong{portion} of a
title, footnote, column label, or cell value — without bolding the
entire line.

\strong{When to use \code{fr_bold()} vs \code{bold = TRUE}:}
\itemize{
\item \code{fr_bold("text")} — inline markup for partial bold within a string.
\item \code{fr_row_style(bold = TRUE)} — bold the entire row uniformly.
\item \code{fr_header(bold = TRUE)} — bold all column headers.
}
}
\section{Rendering}{

In RTF output, bold uses \verb{\\b} ... \verb{\\b0} control words. In future
LaTeX output, it maps to \verb{\\textbf\{\}}. The bold weight matches the
surrounding font family.
}

\examples{
# Bold a label keyword
fr_col("{fr_bold('Total')} Subjects", width = 1.5)

# Bold just the table number in a title:
spec <- tbl_demog |> fr_table()
spec |> fr_titles("{fr_bold('Table 14.1.1')} Summary of Demographics")

}
\seealso{
\code{\link[=fr_italic]{fr_italic()}} for italic, \code{\link[=fr_underline]{fr_underline()}} for underline,
\code{\link[=fr_row_style]{fr_row_style()}} for bolding entire rows, \code{\link[=fr_header]{fr_header()}} for bolding
all column headers.
}
