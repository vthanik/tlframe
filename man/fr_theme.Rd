% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-theme.R
\name{fr_theme}
\alias{fr_theme}
\alias{fr_theme_set}
\title{Set or Update Study-Level Table Theme}
\usage{
fr_theme(
  orientation = NULL,
  paper = NULL,
  margins = NULL,
  col_gap = NULL,
  font_family = NULL,
  font_size = NULL,
  spaces = NULL,
  split = NULL,
  stub = NULL,
  pagehead = NULL,
  pagefoot = NULL,
  tokens = NULL,
  hlines = NULL,
  vlines = NULL,
  spacing = NULL,
  n_format = NULL,
  continuation = NULL,
  page_by_bold = NULL,
  page_by_align = NULL,
  header = NULL,
  group_cont = NULL,
  footnote_separator = NULL
)
}
\arguments{
\item{orientation}{\code{"landscape"} or \code{"portrait"}. \code{NULL} leaves unchanged.}

\item{paper}{\code{"letter"}, \code{"a4"}, or \code{"legal"}. \code{NULL} leaves unchanged.}

\item{margins}{Margin(s) in inches (same formats as \code{\link[=fr_page]{fr_page()}}). \code{NULL}
leaves unchanged.}

\item{col_gap}{Inter-column padding in \strong{points} (integer). \code{NULL} leaves
unchanged. See \code{\link[=fr_page]{fr_page()}} for details.}

\item{font_family}{Font family name. \code{NULL} leaves unchanged.}

\item{font_size}{Font size in points. \code{NULL} leaves unchanged.}

\item{spaces}{How to handle leading spaces in cell data. One of
\code{"indent"} (convert to paragraph-level indent) or \code{"preserve"}
(keep literal spaces). \code{NULL} leaves unchanged. See \code{\link[=fr_cols]{fr_cols()}}
\code{.spaces} parameter for details.}

\item{split}{Logical. \code{TRUE} to enable column splitting for wide tables,
\code{FALSE} to disable. \code{NULL} leaves unchanged. See \code{\link[=fr_cols]{fr_cols()}} \code{.split}
parameter for details.}

\item{stub}{Character vector of column names to mark as stub columns
(repeated in every panel during column splitting). \code{NULL} leaves
unchanged. See \code{\link[=fr_col]{fr_col()}} \code{stub} parameter for details.}

\item{pagehead}{Named list with \code{left}, \code{center}, \code{right}, \code{font_size},
\code{bold} elements — same as \code{\link[=fr_pagehead]{fr_pagehead()}} arguments. \code{NULL} leaves
unchanged.}

\item{pagefoot}{Named list with \code{left}, \code{center}, \code{right}, \code{font_size},
\code{bold} elements — same as \code{\link[=fr_pagefoot]{fr_pagefoot()}} arguments. \code{NULL} leaves
unchanged.}

\item{tokens}{Named list of \code{{token}} values. \code{NULL} leaves unchanged.}

\item{hlines}{Horizontal rule preset string (e.g. \code{"header"}). \code{NULL}
leaves unchanged.}

\item{vlines}{Vertical rule preset string (e.g. \code{"box"}). \code{NULL} leaves
unchanged.}

\item{spacing}{Named list with \code{titles_after}, \code{footnotes_before},
\code{pagehead_after}, \code{pagefoot_before}, \code{page_by_after} (integer
blank lines). \code{NULL} leaves unchanged. See \code{\link[=fr_spacing]{fr_spacing()}}.}

\item{n_format}{A \link[glue:glue]{glue}-style format string for N-count
labels. Applied to all tables that use \code{fr_col(n = ...)} or
\code{fr_cols(.n = ...)} without an explicit \code{.n_format}. Available
tokens: \code{{label}} and \code{{n}}. \code{NULL} leaves unchanged.}

\item{continuation}{Character scalar appended to column headers on
continuation pages (e.g. \code{"(continued)"}). \code{NULL} leaves unchanged.
See \code{\link[=fr_page]{fr_page()}} for details.}

\item{page_by_bold}{Logical. Whether \code{page_by} group labels are bold.
\code{NULL} leaves unchanged. See \code{\link[=fr_rows]{fr_rows()}} for details.}

\item{page_by_align}{Alignment for \code{page_by} group labels. One of
\code{"left"}, \code{"center"}, \code{"right"}, or \code{"decimal"}. \code{NULL} leaves unchanged.
See \code{\link[=fr_rows]{fr_rows()}} for details.}

\item{header}{Named list of header defaults. Currently supports:
\itemize{
\item \code{span_gap} — Logical. Insert gap columns between adjacent spans
(default \code{TRUE}). See \code{\link[=fr_header]{fr_header()}} for details.
\code{NULL} leaves unchanged.
}}

\item{group_cont}{Character scalar appended to group header rows on
continuation pages when a \code{group_by} group spans multiple pages
(e.g. \code{"(continued)"}). \code{NULL} leaves unchanged. See \code{\link[=fr_rows]{fr_rows()}}
for details.}

\item{footnote_separator}{Logical. Whether to draw a separator above the
footnote block. \code{NULL} leaves unchanged.}
}
\value{
Invisibly \code{NULL}.
}
\description{
\code{fr_theme()} sets study-level defaults that are automatically applied to
\strong{every} subsequent \code{fr_table()} call. Define font size, page layout,
tokens, running header/footer, and line rules once at the top of your
program; all tables inherit these settings without repeating them.
}
\details{
This follows the same pattern as \code{ggplot2} theme management. Calling
\code{fr_theme(...)} (or its alias \code{fr_theme_set(...)}) \strong{merges} new settings
into the current global theme without discarding keys you did not mention.
This is analogous to \code{ggplot2::theme_update()}.

Use \code{\link[=fr_theme_reset]{fr_theme_reset()}} to clear all settings and return to built-in
defaults, or \code{\link[=fr_theme_get]{fr_theme_get()}} to inspect the current state.
}
\section{How the theme is applied}{

When \code{fr_table()} is called, it automatically applies the stored theme to
the new \code{fr_spec}. You can still override any setting per-table by calling
the corresponding verb after \code{fr_table()}. Per-table overrides always win.

Theme settings are stored in the package's internal environment and persist
for the duration of the R session. Call \code{\link[=fr_theme_reset]{fr_theme_reset()}} to clear all
settings and \code{\link[=fr_theme_get]{fr_theme_get()}} to inspect the current theme.
}

\examples{
## ── Define study theme at the top of your program ────────────────────────

fr_theme(
  orientation = "landscape",
  paper       = "letter",
  font_size   = 9,
  tokens      = list(study = "TFRM-2024-001", cutoff = "31DEC2024"),
  pagehead    = list(
    left  = "Study: {study}",
    right = "Database Cutoff: {cutoff}"
  ),
  pagefoot    = list(
    left  = "{program}",
    right = "{datetime}"
  ),
  hlines = "header",
  vlines = "box"
)

# All subsequent fr_table() calls inherit the theme — no need to repeat
tbl_demog |>
  fr_table() |>
  fr_titles("Table 14.1.1 Demographics", "Full Analysis Set")

tbl_ae_soc |>
  fr_table() |>
  fr_titles("Table 14.3.2 Adverse Events", "Safety Analysis Set")

## ── Set section spacing (blank lines between table parts) ─────────────────

fr_theme(spacing = list(titles_after = 1L, footnotes_before = 1L))

## ── Merge additional settings (does not discard existing ones) ────────────

fr_theme(font_size = 9, hlines = "header")
fr_theme(vlines = "box")          # adds vlines; font_size and hlines kept
fr_theme_get()                    # inspect

## ── Override a theme setting for one specific table ───────────────────────

tbl_ae_soc |>
  fr_table() |>
  fr_page(orientation = "portrait")   # overrides the landscape theme default

## ── Theme with spacing configuration ──────────────────────────────────────

fr_theme_reset()
fr_theme(
  font_size = 9,
  spacing   = list(titles_after = 2L, footnotes_before = 2L,
                   pagehead_after = 1L)
)
fr_theme_get()$spacing   # check the spacing settings

## ── Theme with leading-space handling ────────────────────────────────────

fr_theme_reset()
fr_theme(spaces = "indent")   # default: leading spaces → paragraph indent
fr_theme_get()$spaces         # "indent"

fr_theme(spaces = "preserve") # keep leading spaces as literal characters
fr_theme_get()$spaces         # "preserve"

## ── Theme with footnote_separator ────────────────────────────────────────

fr_theme_reset()
fr_theme(footnote_separator = FALSE, hlines = "header")
fr_theme_get()$footnote_separator   # FALSE

## ── Theme auto-applied: inspect spec after fr_table() ────────────────────

fr_theme_reset()
fr_theme(font_size = 8, orientation = "portrait", hlines = "header")
spec <- tbl_demog |> fr_table()
spec$page$font_size      # 8 (inherited from theme)
spec$page$orientation    # "portrait" (inherited from theme)

## ── Reset all theme settings ──────────────────────────────────────────────

fr_theme_reset()

}
\seealso{
\code{\link[=fr_theme_get]{fr_theme_get()}} to inspect, \code{\link[=fr_theme_reset]{fr_theme_reset()}} to clear,
\code{\link[=fr_page]{fr_page()}} for per-table page layout, \code{\link[=fr_pagehead]{fr_pagehead()}} and \code{\link[=fr_pagefoot]{fr_pagefoot()}}
for running headers and footers, \code{\link[=fr_spacing]{fr_spacing()}} for per-table gap control.
}
