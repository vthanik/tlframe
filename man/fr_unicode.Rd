% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markup.R
\name{fr_unicode}
\alias{fr_unicode}
\title{Unicode Character by Codepoint}
\usage{
fr_unicode(codepoint)
}
\arguments{
\item{codepoint}{Integer. Unicode codepoint in hex (e.g. \code{0x00B1}
for ±) or decimal (e.g. \code{177L}). Must be a single value.}
}
\value{
An \code{fr_markup} object containing the UTF-8 character.
}
\description{
Inserts a Unicode character by its numeric codepoint. At render time
the backend converts to the appropriate escape sequence (RTF \verb{\\uN}
or LaTeX command). Use this for symbols not available on the keyboard
or for cross-platform portability.

For the most commonly used symbols in clinical tables, prefer the
dedicated helpers: \code{\link[=fr_dagger]{fr_dagger()}}, \code{\link[=fr_ddagger]{fr_ddagger()}}.
}
\section{Common codepoints for clinical tables}{
\tabular{lll}{
   Symbol \tab Hex \tab Description \cr
   ± \tab \code{0x00B1} \tab Plus-minus (lab ranges) \cr
   ≤ \tab \code{0x2264} \tab Less-than-or-equal \cr
   ≥ \tab \code{0x2265} \tab Greater-than-or-equal \cr
   × \tab \code{0x00D7} \tab Multiplication (dosing) \cr
   ° \tab \code{0x00B0} \tab Degree (temperature) \cr
   α \tab \code{0x03B1} \tab Alpha (significance level) \cr
   β \tab \code{0x03B2} \tab Beta (type II error) \cr
   μ \tab \code{0x03BC} \tab Mu (mean) \cr
   † \tab \code{0x2020} \tab Dagger (use \code{\link[=fr_dagger]{fr_dagger()}}) \cr
   ‡ \tab \code{0x2021} \tab Double dagger (use \code{\link[=fr_ddagger]{fr_ddagger()}}) \cr
}
}

\examples{
# Plus-minus symbol in a label
fr_col("Mean {fr_unicode(0x00B1)} SD", width = 1.5)

# Degree symbol
fr_col("Temperature ({fr_unicode(0x00B0)}C)", width = 1.5)

# In a footnote:
spec <- tbl_demog |> fr_table()
spec |> fr_footnotes("[a] P {fr_unicode(0x2264)} 0.05 considered significant.")

}
\seealso{
\code{\link[=fr_dagger]{fr_dagger()}} and \code{\link[=fr_ddagger]{fr_ddagger()}} for common symbol shortcuts,
\code{\link[=fr_super]{fr_super()}} for superscript notation.
}
