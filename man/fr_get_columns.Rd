% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-accessors.R
\name{fr_get_columns}
\alias{fr_get_columns}
\title{Get Column Specifications from a Spec}
\usage{
fr_get_columns(spec)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}} or \code{\link[=fr_listing]{fr_listing()}}.}
}
\value{
A named list of \code{fr_col} objects, keyed by column name. Empty
list if no columns have been configured.
}
\description{
Extracts the named list of \code{fr_col} objects configured via \code{\link[=fr_cols]{fr_cols()}}.
Each element describes a column's label, width, alignment, and other
properties. If no columns have been explicitly configured, returns an
empty list (columns are auto-generated at render time by
\code{finalize_spec()}).
}
\examples{
spec <- tbl_demog |> fr_table() |>
  fr_cols(
    characteristic = fr_col("Characteristic", width = 2.5),
    placebo = fr_col("Placebo", align = "right")
  )
cols <- fr_get_columns(spec)
names(cols)            # "characteristic", "placebo"
cols$placebo$width     # 1.5 (default)
cols$placebo$align     # "right"

}
\seealso{
\code{\link[=fr_cols]{fr_cols()}} to configure columns, \code{\link[=fr_col]{fr_col()}} for the column
spec constructor, \code{\link[=fr_get_col]{fr_get_col()}} for a single column.
}
