% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-style.R
\name{fr_col_style}
\alias{fr_col_style}
\title{Define a Column Style Override}
\usage{
fr_col_style(
  cols = NULL,
  bold = NULL,
  italic = NULL,
  underline = NULL,
  fg = NULL,
  bg = NULL,
  font_size = NULL,
  align = NULL,
  valign = NULL
)
}
\arguments{
\item{cols}{Character vector of column names, or \code{NULL} (all columns).}

\item{bold, italic, underline}{Logical or \code{NULL} to inherit.}

\item{fg}{Foreground (text) colour, or \code{NULL}.}

\item{bg}{Background (fill) colour, or \code{NULL}.}

\item{font_size}{Font size in points, or \code{NULL}.}

\item{align}{Horizontal alignment: \code{"left"}, \code{"center"}, \code{"right"},
\code{"decimal"}, or \code{NULL}. See \code{\link[=fr_style]{fr_style()}} \strong{Alignment model}.}

\item{valign}{Vertical alignment: \code{"top"}, \code{"middle"}, \code{"bottom"}, or
\code{NULL}. See \code{\link[=fr_style]{fr_style()}} \strong{Alignment model}.}
}
\value{
An \code{fr_cell_style} object with \code{type = "col"} for use in
\code{\link[=fr_styles]{fr_styles()}}.
}
\description{
Creates a column-level style object for use in \code{\link[=fr_styles]{fr_styles()}}. Column styles
apply uniformly to all body cells in the targeted columns.
}
\section{Style precedence}{

Column styles are the \strong{broadest} scope and are overridden by both
row styles and cell styles:

\if{html}{\out{<div class="sourceCode">}}\preformatted{fr_col_style  <  fr_row_style  <  fr_style (cell)
}\if{html}{\out{</div>}}

Column \strong{alignment} set via \code{fr_col(align = ...)} in \code{\link[=fr_cols]{fr_cols()}}
is the base default. \code{fr_col_style(align = ...)} overrides it for
body cells. \code{fr_row_style(align = ...)} overrides both. Finally,
\code{fr_style(region = "body", align = ...)} targeting specific cells wins.
}

\section{Tips}{

\itemize{
\item Column alignment is usually set in \code{\link[=fr_cols]{fr_cols()}} via \code{fr_col(align = ...)}.
Use \code{fr_col_style()} only when you need to override alignment for
a subset of columns without reconfiguring the full column spec.
\item Use \code{fr_col_style(cols = "total", bg = "#EBF5FB")} to give the Total
column a distinct background — a common regulatory convention.
\item Column styles apply only to \strong{body} cells. To style header cells,
use \code{fr_style(region = "header", cols = ...)} or \code{\link[=fr_header]{fr_header()}}.
}
}

\examples{
## ── Total column with blue tint ──────────────────────────────────────────

fr_col_style(cols = "total", bg = "#EBF5FB")

## ── Bold the row-label stub column ───────────────────────────────────────

fr_col_style(cols = "characteristic", bold = TRUE)

## ── Right-align all numeric data columns ─────────────────────────────────

fr_col_style(cols = c("zom_50mg", "zom_100mg", "placebo", "total"),
             align = "right")

## ── Full pipeline with column styles ─────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_hlines("header") |>
  fr_vlines("box") |>
  fr_styles(
    fr_style(region = "header", bold = TRUE),
    fr_col_style(cols = "total", bg = "#EBF5FB")
  )

}
\seealso{
\code{\link[=fr_row_style]{fr_row_style()}} for row-level styling, \code{\link[=fr_style]{fr_style()}} for
cell-level styling, \code{\link[=fr_styles]{fr_styles()}} to apply to a spec.
}
