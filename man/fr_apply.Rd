% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-recipe.R
\name{fr_apply}
\alias{fr_apply}
\title{Apply a Recipe to a Spec}
\usage{
fr_apply(spec, recipe)
}
\arguments{
\item{spec}{An \code{fr_spec} object from \code{\link[=fr_table]{fr_table()}}.}

\item{recipe}{An \code{fr_recipe} object from \code{\link[=fr_recipe]{fr_recipe()}}.}
}
\value{
A modified \code{fr_spec}.
}
\description{
Applies an \code{fr_recipe} to an \code{fr_spec}, evaluating each captured verb call
in sequence with the spec as the first argument. This is the primary way
to use recipes in a pipeline.
}
\examples{
# Basic application
style <- fr_recipe(fr_hlines("header"), fr_header(bold = TRUE))
spec <- tbl_demog |> fr_table() |> fr_apply(style)

# Apply a recipe and then override one parameter with a direct verb call
spec <- tbl_ae_soc |>
  fr_table() |>
  fr_apply(style) |>
  fr_header(bold = FALSE)   # override the recipe's bold = TRUE

# Apply to different datasets with the same recipe
demog_spec <- tbl_demog |> fr_table() |> fr_apply(style)
disp_spec  <- tbl_disp  |> fr_table() |> fr_apply(style)

}
\seealso{
\code{\link[=fr_recipe]{fr_recipe()}}
}
