% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-style.R
\name{fr_row_style}
\alias{fr_row_style}
\title{Define a Row Style Override}
\usage{
fr_row_style(
  rows = NULL,
  bold = NULL,
  italic = NULL,
  underline = NULL,
  color = NULL,
  background = NULL,
  font_size = NULL,
  align = NULL,
  valign = NULL,
  height = NULL
)
}
\arguments{
\item{rows}{Integer vector of body row positions, \code{"all"}, \code{NULL} (all
body rows), or a group header selector. Row indices are 1-based.
Multiple indices are supported: \code{rows = c(1L, 3L, 5L)}.

\strong{Group header selectors} (resolved at render time):
\itemize{
\item \code{"group_headers"} — all group header rows (from \code{group_by} with
\code{label} or \code{leaf}).
\item \code{"group_headers:<level>"} — only headers at a specific hierarchy
level (e.g., \code{"group_headers:soc"}). For \code{leaf} hierarchies only.
}}

\item{bold, italic, underline}{Logical or \code{NULL} to inherit.}

\item{color}{Foreground (text) colour, or \code{NULL}.}

\item{background}{Background (fill) colour, or \code{NULL}.}

\item{font_size}{Font size in points, or \code{NULL}.}

\item{align}{Horizontal alignment: \code{"left"}, \code{"center"}, \code{"right"},
\code{"decimal"}, or \code{NULL}. See \code{\link[=fr_style]{fr_style()}} \strong{Alignment model}.}

\item{valign}{Vertical alignment: \code{"top"}, \code{"middle"}, \code{"bottom"}, or
\code{NULL}. See \code{\link[=fr_style]{fr_style()}} \strong{Alignment model}.}

\item{height}{Row height in inches, or \code{NULL} (auto).}
}
\value{
An \code{fr_cell_style} object with \code{type = "row"} for use in
\code{\link[=fr_styles]{fr_styles()}}.
}
\description{
Creates a row-level style object for use in \code{\link[=fr_styles]{fr_styles()}}. Row styles apply
uniformly across all cells in the targeted rows.
}
\section{Style precedence}{

When multiple styles target the same cell, narrower scopes win:

\if{html}{\out{<div class="sourceCode">}}\preformatted{fr_col_style  <  fr_row_style  <  fr_style (cell)
}\if{html}{\out{</div>}}

Within the same scope level, later styles (later in the \code{fr_styles()}
call or in later \code{fr_styles()} calls) override earlier ones.
\code{fr_row_style()} overrides \code{fr_col_style()} for the same property
because row styles are narrower than column styles.
}

\section{Tips}{

\itemize{
\item Use \code{fr_row_style(rows = "all", background = "#F5F5F5")} for a subtle
background on all body rows (zebra striping requires alternating calls:
rows of odd/even index).
\item Row height in regulatory tables is normally controlled by \code{fr_page(font_size = ...)}
and the layout engine. Set \code{height} only when you need a specific row
to be taller (e.g. a summary row).
\item Use \code{\link[=fr_rows_matches]{fr_rows_matches()}} instead of hard-coded row indices for
content-based row targeting (e.g. bold every "Total" row).
}
}

\examples{
## ── Standalone: store and reuse ─────────────────────────────────────────
bold_first <- fr_row_style(rows = 1L, bold = TRUE)
bold_first  # inspect

## ── Bold the first body row (e.g. total row) ─────────────────────────────

fr_row_style(rows = 1L, bold = TRUE)

## ── Light grey background on all body rows ────────────────────────────────

fr_row_style(rows = "all", background = "#F5F5F5")

## ── Highlight multiple specific rows ──────────────────────────────────────

# Pass a vector of row indices to style multiple disparate rows at once:
fr_row_style(rows = c(2L, 4L, 6L), background = "#E8F4FD")

## ── Highlight last row (totals) in a different colour ─────────────────────

fr_row_style(rows = nrow(tbl_demog), background = "#FFF3CD", bold = TRUE)

## ── Increase height of header-adjacent row ────────────────────────────────

fr_row_style(rows = 1L, height = 0.3)

## ── Full pipeline with row styles ─────────────────────────────────────────

tbl_demog |>
  fr_table() |>
  fr_hlines("header") |>
  fr_styles(
    fr_row_style(rows = "all", background = "#FAFAFA"),
    fr_style(region = "header", bold = TRUE, background = "#E0E0E0")
  )

## ── Bold all group header rows ────────────────────────────────────────

data.frame(
  variable = c("Sex", "Sex", "Age", "Age"),
  stat = c("Female", "Male", "Mean (SD)", "Median"),
  value = c("27 (60.0)", "18 (40.0)", "75.0 (6.8)", "74.0"),
  stringsAsFactors = FALSE
) |>
  fr_table() |>
  fr_cols(variable = fr_col(visible = FALSE)) |>
  fr_rows(group_by = list(cols = "variable", label = "stat")) |>
  fr_styles(
    fr_row_style(rows = "group_headers", bold = TRUE, background = "#E8E8E8")
  )

}
\seealso{
\code{\link[=fr_col_style]{fr_col_style()}} for column-level styling, \code{\link[=fr_style]{fr_style()}} for
cell-level styling, \code{\link[=fr_styles]{fr_styles()}} to apply to a spec.
}
